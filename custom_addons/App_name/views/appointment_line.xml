<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="view_line_list" model="ir.ui.view">
        <field name="name">appointment.line</field>
        <field name="model">first.line</field>
        <field name="arch" type="xml">
            <list string="User Tags" editable="top">
                <field name="appointment_id"/>
                <field name="product_id"/>
                <field name="quantity"/>
            </list>
        </field>
    </record>

    <record id="action_user_line" model="ir.actions.act_window">
        <field name="name">appointment_line</field>
        <field name="res_model">first.line</field>
        <field name="view_mode">list</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create a new appointment line
            </p>
        </field>
    </record>
</odoo>